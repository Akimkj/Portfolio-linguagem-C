#include <stdio.h>
#include <stdlib.h>

int main() {
    int idade;
    float peso, altura;
    char sexo;
    printf("Informe, respectivamente, a sua idade, sexo(m, f ou n), peso e altura: ");
    scanf("%d", &idade);
    printf("%d", idade);
}